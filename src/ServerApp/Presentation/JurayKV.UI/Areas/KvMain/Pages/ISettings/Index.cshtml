@page
@model JurayKV.UI.Areas.KvMain.Pages.ISetting.IndexModel
@{
}
<div class="card">
    <div class="card-header">
        <h3 class="card-title">SETTING</h3>


        @*<a asp-page="./Add" class="btn bg-gradient-warning btn-sm" style="float:right;">Add New</a>*@

    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <div class="table-responsive">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>S/N</th>
                        
                        <th>
                            @Html.DisplayNameFor(model => model.Setting[0].MinimumAmountBudget)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Setting[0].DefaultAmountPerView)
                        </th>
                         
                        <th></th>
                    </tr>
                </thead>

                @{
                    int sn = 0;
                }
                <tbody>
                    @foreach (var item in Model.Setting)
                    {
                        <tr>

                            <td>
                                @{
                                    sn++;
                                }
                                @sn
                            </td>
                            
                            <td>
                                @Html.DisplayFor(modelItem => item.MinimumAmountBudget)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DefaultAmountPerView)
                            </td>
                           
                            <td>
                                <div class="btn-group">
                                    <button type="button" class="btn btn-success">Action</button>

                                    <div class="btn-group">
                                        <button type="button" class="btn btn-success dropdown-toggle dropdown-icon" data-toggle="dropdown" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" style="">

                                            <a class="dropdown-item" asp-page="./Update" asp-route-id="@item.Id">Edit</a>

                                        </div>
                                    </div>
                                </div>

                            </td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
